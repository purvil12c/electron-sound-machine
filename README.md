# A simple sound machine front-end based on Electron.js JavaScript Framework.

Steps for running the project: 
1. Clone the project
2. Within the directory, run "npm install"
3. Within the directory, run "npm start"
